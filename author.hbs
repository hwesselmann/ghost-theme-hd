{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}

{{! The big featured header }}

{{! Everything inside the #author tags pulls data from the author }}
{{#author}}
    <header class="main-header author-head {{#if cover}}" style="background-image: url({{cover}}){{else}}no-cover{{/if}}">
        <nav class="main-nav overlay clearfix">
            <a class="back-button" href="{{@blog.url}}"><i class="fa fa-chevron-left"></i>&nbsp;Home</a>
            <a class="subscribe-button" href="{{url}}rss/"><i class="fa fa-rss"></i>&nbsp;{{name}}</a>
        </nav>
    </header>

    <section class="author-profile inner">
        {{#if image}}
        <figure class="author-image">
            <div class="img" style="background-image: url({{image}})"><span class="hidden">{{name}}'s Picture</span></div>
        </figure>
        {{/if}}
        <h1 class="author-title">{{name}}</h1>
        <h2 class="author-bio">{{bio}}</h2>
        <div class="author-meta">
            {{#if location}}<span class="author-location"><i class="fa fa-map-marker"></i>&nbsp;{{location}}</span>{{/if}}
            {{#if website}}<span class="author-link"><a href="{{website}}"><i class="fa fa-external-link"></i>&nbsp;{{website}}</a></span>{{/if}}
            <span class="author-stats"><i class="fa fa-bar-chart"></i>&nbsp; {{plural ../pagination.total empty='keine artikel' singular='% artikel' plural='% artikel'}}</span>
        </div>
    </section>
{{/author}}

{{! The main content area on the homepage }}
<main class="content" role="main">

    {{! The tag below includes the post loop - partials/loop.hbs }}
    {{> "loop"}}

</main>